# URL publique du site : sert aux liens absolus (OpenGraph, fichiers .ics).
# Facultative — sans elle, Vercel utilise l'URL du déploiement et le local
# retombe sur http://localhost:3000.
# À ne PAS définir vide : soit une vraie URL, soit rien du tout.
NEXT_PUBLIC_SITE_URL=https://bookees.fr
